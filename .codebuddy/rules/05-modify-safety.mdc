---
description: 代码修改安全红线——改他人代码先确认、公共代码慎重、重构≠重写、删除前三思;防止 AI 擅自改动调用链上下游
alwaysApply: true
enabled: true
provider: project
---

# 代码修改安全红线（05-modify-safety）

> 本文件是 **AI 修改已有代码时的强制约束**。
> 写新代码 vs 改老代码,安全性标准差距巨大。

---

## 1. 🔴 四条总红线

| 场景 | 规则 |
|------|------|
| **修改他人代码** | ❌ 禁止随意修改非本人编写的业务逻辑代码。如需修改,必须先与原作者确认,获得同意后方可改动。 |
| **修改公共 / 框架代码** | ❌ 禁止修改公共库 / 框架核心 util/base 类,除非明确确认影响范围。 |
| **重构 ≠ 重写** | ❌ 禁止以"优化"名义重写整个方法,优先局部修改、最小化 diff。 |
| **删除代码** | ❌ 禁止删除看似"没用"的方法或字段——它可能被反射、序列化、前端模板引用。 |

---

## 2. 如何判断代码归属

### 2.1 三种判断方式

| 方式 | 操作 |
|------|------|
| **@author 注释** | 查看类 / 方法上的 `@author` 标签(参 `02-code-comments.mdc` 注释规范) |
| **Git Blame** | `git blame <file>` 确认原作者 |
| **最近改动** | `git log -p <file>` 看近期 commit |

### 2.2 默认行为

> 不确定归属时,**默认视为「他人代码」**,先问后改。
> 即使原作者是你自己,只要是「早期写的」,也要**回读当下需求**,不盲目复用。

---

## 3. 修改前必做清单

```
发起修改
  ↓
[1] 读懂现有代码(不要只看表象)
  ↓
[2] 搜调用方:grep / IDE references,列出所有受影响位置
  ↓
[3] 评估影响范围:跨模块?跨服务?跨端(PC/小程序/移动)?
  ↓
[4] 给用户回报影响清单 → 获得明确确认
  ↓
[5] 修改 + 验证(单元测试 / 集成测试 / 灰度)
  ↓
[6] 回报:改了哪里,影响什么,验证结果
```

---

## 4. AI 行为红线(🔴 禁止)

### 4.1 禁止的修改行为

| 行为 | 后果 |
|------|------|
| 🔴 仅凭推测擅自改动调用链上下游代码 | 容易引入隐性 bug |
| 🔴 重命名公共方法 / 类 / 字段而不更新所有引用 | 编译失败或运行时调用失败 |
| 🔴 修改公共 utils / framework base 类 | 影响不可控 |
| 🔴 删除看似无用的代码、注释、字段 | 可能被反射、序列化、模板引用 |
| 🔴 在循环外复用同一对象却不重新初始化 | 状态污染 |

### 4.2 必须显式确认的修改

下列修改**必须**先列出影响清单,得到用户确认后执行:

```
[1] 列表所有受影响的方法和调用链
[2] 向用户说明每个改动点的原因和影响范围
[3] 获得用户明确确认后,才能执行修改
```

---

## 5. 重构的正确姿势

### 5.1 何时该重构

- ✅ 用户明确要求重构
- ✅ 同一段逻辑在 3 处以上重复
- ✅ 明确影响性能 / 可维护性 / 安全
- ✅ 当前修改必然要重构才能继续

### 5.2 如何重构

| 步骤 | 动作 |
|------|------|
| 1 | 跑现有测试(确保基线绿) |
| 2 | 抽取小函数 / 提取类,**保留原方法签名**(兼容外部调用) |
| 3 | 把新方法接到原方法或新接口,**旧路径暂不下线** |
| 4 | 增加单测覆盖新方法 |
| 5 | 全量回归测试 |
| 6 | 灰度 / 观察一段时间后再删旧路径 |

### 5.3 🔴 反模式

- ❌ "为了整洁"一次性重写整个 service
- ❌ 改方法签名但不更新所有调用方
- ❌ 用私有 util 替换第三方依赖库时不做兼容层
- ❌ 把已发布 API 直接改成新接口(应增加 v2)

---

## 6. 删除代码的安全姿势

### 6.1 删除前的核查清单

```
[1] 全文 grep 该标识符(类名/方法名/字段名)
[2] IDE 全工程 reference search
[3] 检查序列化:JSON 字段名 / DO 字段 / DB 列
[4] 检查模板:HTML / Vue template / 邮件模板
[5] 检查反射:Class.forName / getMethod / @JsonAnySetter
[6] 检查配置:YAML / properties / 环境变量
[7] 检查迁移脚本 / 数据库存储过程
[8] 确认无引用 → 列出删除项 → 用户确认 → 再删
```

### 6.2 严禁(🔴)

- ❌ 删除未引用的「看似没用」的方法
- ❌ 删除时连带删除 import 行 / 测试代码
- ❌ 直接 `git clean -fd` 清理「看起来像临时文件」
- ❌ 不用 grep 全工程就删除「已重命名」的旧名

---

## 7. 公共代码 vs 私有代码

| 类型 | 修改原则 |
|------|----------|
| **私有方法 / 内部类** | 修改前简单回读,影响范围小 |
| **公共方法 / 公开 API** | 影响范围大,先列影响 → 确认 → 改 |
| **基础类 / 框架类** | 禁止直接改,可继承扩展或 wrapper |
| **第三方依赖** | 🔴 禁止修改 vendor / node_modules / 编译产物 |

---

## 8. 跨层调用的修改红线

### 8.1 后端跨层

```
❌ Controller → Mapper 直接调用
❌ Service 跨层调 Controller
✅ Controller → Service → Mapper
```

修改任何一层时,**只改本层职责**,不要连带改他层逻辑。

### 8.2 跨端调用

修改服务端 API 时,要同步评估影响端:
- PC 管理端
- 移动管理端(uni-app)
- 小程序端 / 企微端
- 第三方对接系统

---

## 9. 单测与回滚策略

### 9.1 必备条件

| 改动类型 | 必备 |
|---------|------|
| 修改业务方法 | 修改前后单元测试都通过 |
| 重构公共方法 | 加新单测覆盖新方法,**老方法保留** |
| 删除代码 | **已确认所有引用都下线**且单测覆盖空白处 |

### 9.2 回滚方案

| 改动 | 回滚手段 |
|------|----------|
| 普通代码 | Git revert / reset 到上一个 commit |
| 数据库字段 | 提供反向 DDL(`DROP COLUMN` / `RENAME`) |
| 配置变更 | 保留原配置项,新配置项并行运行 |
| 公共 API | 保留旧方法,标记 `@Deprecated`,灰度切换 |

---

## 10. 修改完成后的回报

AI 完成代码修改,**必须**主动回报:

```markdown
## 修改汇报

### 改动文件
- src/xxx/UserService.java  (Line 45-60: 修复并发条件下未初始化问题)
- src/xxx/UserServiceTest.java  (新增回归测试)

### 改动原因
修复 Issue #123:`getUserById` 在并发场景下偶发返回未提交数据

### 影响范围
- 影响:`UserService.getUserById` 调用方共 5 处
- 已在 XxxService / YyyController 更新引用

### 验证
- ✅ 单元测试通过(新增 2 个回归用例)
- ✅ 编译通过
- ⚠️ 未跑集成测试(需要部署环境)
```

---

## 11. 一句话总结

> **改代码前先问「原作者会同意吗?」+「所有引用方受影响吗?」+「回滚怎么搞?」三个问题**。
> 三个都是肯定的,才动手。
