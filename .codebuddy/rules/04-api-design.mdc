---
description: API / 接口设计总则——RESTful 路径、HTTP 方法、统一响应格式、权限标识、错误码、版本控制(跨语言/跨框架通用层)
alwaysApply: true
enabled: true
provider: project
---

# API / 接口设计总则（04-api-design）

> 本文件是跨语言通用的 API 设计原则(后端 + 前端请求层共同遵守)。
> 前端请求层细节见 `fe-react.mdc`;本项目接口信封唯一事实源见 `proj-nextjs-stack.mdc` §3 与 `docs/api-contract.md`。

> ⚠️ **本项目差异(优先于下文通用规则)**,依据 `proj-nextjs-stack` §3:
> 1. 业务 `code` 与 HTTP 状态码**一一对应**(仅 `0 / 400 / 404 / 500 / 503` 五个),**不是**「HTTP 200 + 响应体业务码」模式
> 2. 信封逐字固定为 `{ code, message, data, traceId }`,**禁止**增删字段或改名;**禁止**新增错误码(含 501)
> 3. 分页字段为 `page` / `pageSize`(不是 `pageNo`),默认 `1` / `20`,`pageSize` 上限 `100`
> 4. 接口路径仅契约 7 条且**禁止新增**;当前无 `/api/v1` 版本段,**禁止**擅自加版本前缀
>
> 下文通用原则仅在与上述不冲突时参考。

---

## 1. RESTful 路径风格

### 1.1 资源命名

| 维度 | 约定 |
|------|------|
| 路径主体 | **名词**(复数) `/users`、`/orders` |
| 层级关系 | 嵌套: `/users/{id}/orders` |
| 自定义动作 | 副词+动词:`/users/{id}/activate` / `/orders/{id}/cancel` |
| 大小写 | 全部小写,下划线分隔,避免驼峰 |
| 文件扩展名 | 路径中不带 `.json` / `.xml`,通过 Content-Type 区分 |

### 1.2 操作映射

| 操作 | HTTP 方法 | 路径模板 |
|------|-----------|----------|
| 列表 / 分页 | `GET` | `/users` |
| 详情 | `GET` | `/users/{id}` |
| 新增 | `POST` | `/users` |
| 全量更新 | `PUT` | `/users/{id}` |
| 部分更新 | `PATCH` | `/users/{id}` |
| 删除(单条) | `DELETE` | `/users/{id}` |
| 批量删除 | `DELETE` | `/users?ids=1,2,3` 或 `/users/batch` |
| 导出 | `GET` | `/users/export` |
| 导入 | `POST` | `/users/import` |

### 1.3 URL 设计红线

- ❌ **禁止**动词路径:`/getUser` / `/createUser`(应 `GET /users/{id}` `POST /users`)
- ❌ **禁止** URL 中带函数命名风格:驼峰、空格
- ❌ **禁止** URL 中带敏感信息(密码 / token)
- ✅ 路径名要稳定,业务字段(如状态)放 query 参数,**不放路径**

---

## 2. HTTP 状态码

| 状态码 | 含义 | 使用场景 |
|--------|------|---------|
| 200 | OK | 一切成功(不管业务 code) |
| 201 | Created | 资源创建成功 |
| 204 | No Content | 删除成功 / 无返回数据 |
| 400 | Bad Request | 参数校验失败 |
| 401 | Unauthorized | 未登录 / token 失效 |
| 403 | Forbidden | 无权限 |
| 404 | Not Found | 资源不存在 |
| 405 | Method Not Allowed | HTTP 方法不允许 |
| 409 | Conflict | 资源冲突 / 数据已存在 |
| 422 | Unprocessable Entity | 语义错误(参数合法但逻辑不可处理) |
| 429 | Too Many Requests | 频率限制 |
| 500 | Internal Server Error | 服务器内部错误 |
| 503 | Service Unavailable | 服务不可用(熔断 / 限流) |

> **注意**:HTTP 状态码是**传输层**语义,**业务逻辑错误**应放在响应体中的 `code` 字段。
> 例:`HTTP 200` + `{code: 40001, message: "用户不存在"}`。

---

## 3. 统一响应格式

### 3.1 通用模板

```json
{
  "code": 0,                        // 业务状态码:0=成功,其他=失败
  "data": <T>,                      // 业务数据,可为对象 / 数组 / null
  "message": "success" | "错误描述"  // 用户可读信息,失败时必填
}
```

### 3.2 分页响应

```json
{
  "code": 0,
  "data": {
    "list": [...],
    "total": 100,
    "pageNo": 1,
    "pageSize": 10
  },
  "message": "success"
}
```

### 3.3 错误响应

```json
{
  "code": 40101,
  "data": null,
  "message": "登录已过期,请重新登录"
}
```

### 3.4 各语言通用建议

| 语言 | 推荐封装 |
|------|----------|
| Java (Spring) | `R<T>` / `CommonResult<T>` / `Result<T>` |
| Python (FastAPI) | 自定义 `ResponseModel[T]` + `JSONResponse` |
| Node (Nest) | `ApiResponse<T>` interceptor |
| TS (前端调用) | `ApiResult<T>` 类型 + axios interceptor 解析 |

> **团队统一一种格式**,避免同一项目出现多种响应结构。

---

## 4. 业务错误码设计

### 4.1 编码规则(推荐)

```
{category}{subcategory}{sequence}

例:
  0       = 成功
  1xxxx   = 通用错误
  2xxxx   = 鉴权 / 权限错误
  3xxxx   = 业务模块 A 错误
  4xxxx   = 业务模块 B 错误
  5xxxx   = 第三方 / 外部错误
  9xxxx   = 系统未知错误
```

### 4.2 错误码表(示例)

| code | 含义 |
|------|------|
| 0 | 成功 |
| 10001 | 参数校验失败 |
| 10002 | 请求方法不允许 |
| 10100 | 资源不存在 |
| 20001 | 未登录 |
| 20003 | 无权限 |
| 30001~30099 | 用户模块错误 |
| 30101~30199 | 订单模块错误 |
| 90001 | 系统内部错误 |

### 4.3 错误信息规范

- ✅ 中文,简洁,**用户可读**
- ✅ 必要时包含可操作建议("请检查 X")
- ❌ 禁止暴露栈信息 / SQL 语句 / 内部表名

---

## 5. 权限标识格式

### 5.1 推荐格式

```
{module}:{biz}:{action}

module: 模块缩写(如 sys / erp / crm / order / content)
biz:    业务对象(如 user / product / order)
action: 操作(create / update / delete / query / export / import / approve)

例:
  sys:user:create
  erp:product:export
  order:refund:approve
```

### 5.2 权限红线(🔴)

- ❌ **禁止** 在前端硬编码业务逻辑判断代替权限控制(前后端都必须鉴权)
- ❌ **禁止** 权限标识在生产环境使用通配符匹配(`*:*`)
- ✅ 后端「每个」写接口必须有 `@PreAuthorize` / `@Permission` 等注解
- ✅ 前端「每个」写按钮必须有 `v-permission` / `<Auth>` 包裹

---

## 6. 入参 / 出参设计原则

### 6.1 入参

| 原则 | 说明 |
|------|------|
| **请求体** | `POST` / `PUT` / `PATCH` 用 `application/json` body |
| **查询参数** | `GET` 用 query string,复杂查询可 `POST` + body |
| **字段命名** | 与数据库保持一致(蛇形 vs 驼峰按项目),**跨层不混用** |
| **分页参数** | `pageNo` / `pageSize` 或 `page` / `size`(项目统一) |
| **时间字段** | ISO 8601 字符串 / 时间戳,统一 |

### 6.2 出参

| 原则 | 说明 |
|------|------|
| **VO 转换** | DB DO 不直接返回给前端,必须转 VO |
| **脱敏** | 手机号 / 身份证 / 银行卡 列表必须脱敏 |
| **时间格式** | 项目内统一(ISO / `yyyy-MM-dd HH:mm:ss`) |
| **空值** | 不输出 `null` 字段(除非业务需要),空集合返回 `[]` |

---

## 7. API 版本控制

### 7.1 路径版本(推荐)

```
/api/v1/users
/api/v2/users
```

### 7.2 头版本(可选)

```
Accept: application/vnd.myapi.v1+json
```

### 7.3 何时新增版本

- 破坏性变更(字段删除 / 类型变更 / 语义变化)→ 新版本
- 字段新增 / 字段可空 → 老版本兼容,不升版

---

## 8. 限流 / 防刷 / 幂等

| 能力 | 实现 |
|------|------|
| **限流** | 中间件级(Nginx / Spring Cloud Gateway / API Gateway) |
| **防刷** | 同一 IP / 用户 滑动窗口限频 |
| **幂等** | 写接口支持 `Idempotency-Key` 头 / 业务唯一键 |
| **超时** | 默认 30s,长任务改异步 + 任务 ID |

---

## 9. 接口安全(CORS / CSRF / XSS)

| 场景 | 处理 |
|------|------|
| 跨域 | 服务端 `Access-Control-Allow-*`,不允许 `*` 通配带凭证 |
| CSRF | 写操作要求 `X-CSRF-Token` 或 SameSite Cookie |
| XSS | 响应 HTML 时 escape;前端 `v-html` 严禁渲染用户输入 |
| SQL 注入 | 参数化查询,禁止字符串拼接 |

---

## 10. OpenAPI / 接口文档

- 🔴 所有 API 必须有同步维护的接口文档(Swagger / OpenAPI / Apifox)
- ✅ 每个接口标注:用途 / 入参示例 / 出参示例 / 错误码列表 / 权限标识
- ✅ 文档与代码同仓库(`docs/api/` 或内置 Swagger UI)

---

## 11. 一句话总结

> **资源用名词,操作看 HTTP 动词,业务码放响应体,权限标识统一格式,写接口必鉴权必防重放。**
