---
description: 跨语言通用编码总则——命名约定、函数规模、类型系统、错误处理、复杂度控制、跨语言通用红线,与具体语言/框架无关
alwaysApply: true
enabled: true
provider: project
---

# 通用编码总则（01-coding-style）

> 本文件是**跨语言通用**编码风格。本项目(Next.js 15 + React 19 + TypeScript)相关细则见:
> - React / Next.js → `fe-react.mdc`
> - 样式 → `fe-css-tailwind.mdc`
> - SQL → `lang-sql.mdc`(PostgreSQL,含本项目差异)
> - 项目目录 / 命名 / 接口信封 → `proj-nextjs-stack.mdc`
>
> 🔴 与 `proj-*` 冲突时,**一律以 `proj-*` 为准**。

---

## 1. 命名约定

### 1.1 通用原则

| 对象 | 约定 | 例子 |
|------|------|------|
| 常量 | `UPPER_SNAKE_CASE` | `MAX_RETRY_COUNT` |
| 变量 / 函数 / 方法 | `camelCase` | `getUserById`、`maxRetry` |
| 类 / 类型 / 接口 / 组件 | `PascalCase` | `UserService`、`UserVO`、`CustomerForm` |
| 文件名(类) | 与类名一致,PascalCase | `UserService.java`、`CustomerForm.vue` |
| 文件名(工具/常量) | `kebab-case` | `string-utils.ts`、`app-config.ts` |
| 布尔变量 | `is` / `has` / `can` / `should` 前缀 | `isDeleted`、`hasPermission`、`canEdit` |
| 异步函数 | `动词 + 名词` 或 `动词_异步` 标识 | `fetchUser`、`loadUserAsync` |
| 私有成员 | 前缀 `_` 或模块私有 | `_internal`、`#internal` |

### 1.2 命名红线(🔴 强制)

- ❌ **禁止**用 `data` / `info` / `result` / `temp` / `tmp` / `xxx` / `test` 等模糊命名
- ❌ **禁止**单字母变量(`a`, `b`, `x` 等,`i` / `j` 等循环变量除外)
- ❌ **禁止**中文拼音变量名
- ❌ **禁止**混用命名风格(同项目内统一)
- ✅ 命名要「**自解释**」——读名知意,无须注释

### 1.3 命名优先级

遇到命名冲突时优先级:
1. **业务约定名**(已在团队文档中沉淀) > 通用规范
2. **项目约定名** > 通用规范
3. **类型 / 类名**(PascalCase) > 函数 / 变量

---

## 2. 函数规模

| 指标 | 上限 | 触发动作 |
|------|------|---------|
| 单函数行数 | **≤ 30 行**(不含空行注释) | 超限必须拆分 |
| 函数参数 | **≤ 4 个** | 超限改为参数对象 |
| 函数嵌套深度 | **≤ 3 层** | 超限抽取函数 |
| 单文件行数 | **≤ 500 行** | 超限按职责拆文件 |
| 单文件类 / 接口 / 组件 | **1 个核心 + 辅助** | 超限拆模块 |

**例外**:
- 单测函数可超过 30 行(测试代码复杂度稍宽)
- 数据定义文件(`types.ts`、`constants.ts`)可放多个常量

---

## 3. 类型系统

### 3.1 默认要求

| 语言 | 要求 |
|------|------|
| TypeScript | **必须** 开启 `strict: true`,禁止 `any` 消音(用 `unknown` + 收窄) |
| SQL | 字段必须带类型与注释,详见 `lang-sql.mdc` |

### 3.2 类型红线(🔴 强制)

- ❌ **禁止** TS 用 `any`(必要时用 `unknown` + 类型守卫)
- ❌ **禁止** 宽松类型接收所有值(`@ts-ignore` / `@ts-expect-error` / Python `Any` 滥用)
- ✅ 复杂结构必须定义为 `interface` / `type` / `class`

### 3.3 枚举与常量

- 业务枚举值必须用枚举类型,不允许直接 magic number
- 状态值 0/1/2 之类的,**`@deprecated` 或注释说明枚举含义**

---

## 4. 错误处理

### 4.1 总原则

| 维度 | 策略 |
|------|------|
| **何时抛** | 边界检查失败 + 业务校验失败 + 权限拒绝 → 必须抛 |
| **何时吞** | 仅在「不可恢复 + 用户已显式确认」时静默处理 |
| **何时返回错误值** | 类型系统支持 Result/Either 时优先于异常 |
| **日志** | 控制台用英文日志,用户提示用中文 |

### 4.2 错误处理红线(🔴 强制)

- ❌ **禁止** catch 后只 `e.printStackTrace()` / `console.log(err)` 后继续运行
- ❌ **禁止** 静默 `catch {}` / `except: pass` / 空 catch 块
- ❌ **禁止** 用 magic value (`-1` / `null` / `undefined`) 表示错误
- ✅ 必须**处理边界**:null、空集合、超大数值、网络超时、外部输入
- ✅ 自定义错误类型优先,语义明确优于抛字符串

### 4.3 错误信息规范

```
[模块名] [操作] [原因] [建议]
```

```ts
// 业务错误统一抛 ApiError,由 withRouteHandler 统一转信封
throw new ApiError(ErrorCode.NOT_FOUND, '公告不存在');
```

---

## 5. 并发与异步

| 规则 | 描述 |
|------|------|
| **共享变量** | 必须明确同步原语(锁 / atomic / channel) |
| **回调地狱** | 必须 async/await / Promise / 协程扁平化 |
| **异步错误** | try/catch 必须覆盖 await |
| **资源清理** | defer / using / finally / context manager 显式释放 |
| **超时** | 所有 IO 必须显式超时,默认 5~30s |

---

## 6. 模块化与依赖

| 规则 | 描述 |
|------|------|
| **单一职责** | 一个文件 / 类 / 模块只做一件事 |
| **依赖方向** | 上层 → 下层,禁止反向依赖 |
| **循环依赖** | 🔴 禁止 |
| **公共工具** | 收口到 `utils/` / `lib/` / `common/`,禁止多处复制 |
| **第三方依赖** | 引入前确认维护活跃度 + License + 体积 |

---

## 7. 代码复杂度

```
圈复杂度(Cyclomatic) ≤ 10
认知复杂度(Cognitive) ≤ 15
```

**降复杂度手段**(优先级从高到低):
1. 早返回(early return)减少嵌套
2. 抽取小函数
3. 用表驱动 / 策略模式替代 if-else 链
4. 用多态替代类型判断

---

## 8. 函数式与 OO 风格选择

| 场景 | 推荐 |
|------|------|
| 数据转换流水线 | 函数式(map / filter / reduce / pipe) |
| 业务实体(状态+行为) | OO(class) |
| 工具函数 / 无状态 | 函数式 |
| React 组件 | 声明式(函数组件) |
| 数据访问层(`lib/data`) | OO(模块化函数集) |
| 算法核心 | 看场景,优先可读 |

**例外**:
- 团队历史沉淀风格优先
- 性能关键路径优先(有时 mutable 更快)

---

## 9. 不可变 vs 可变

- ✅ **默认不可变**(`const` / `final` / `readonly` / `frozen`)
- ❌ **禁止** 在多个地方引用同一可变对象再并发修改
- ✅ 必须修改变量时,先 `clone()` / `copy()` / `[...spread]`

---

## 10. 测试覆盖

| 类型 | 最低覆盖率 |
|------|-----------|
| 工具函数(`lib/utils`) | 80% |
| 数据访问层(`lib/data`) | 70% |
| Route Handler / 校验 schema | 50% |
| UI 组件 | 关键交互路径必须有 |

**红线**(🔴):
- 涉及金额计算 / 权限 / 支付 的函数 **必须** 单测
- Bug 修复后必须有回归测试

---

## 11. 版本控制与 Git

| 维度 | 约定 |
|------|------|
| 提交粒度 | 一个原子变更 = 一个 commit |
| 提交信息 | `<type>(<scope>): <subject>`,type: feat / fix / refactor / docs / test / chore |
| 分支 | feature / fix / hotfix / release |
| 合入前 | 单测通过 + 至少 1 名 reviewer |
| 禁止 | 提交注释掉的代码、调试日志、敏感信息(密码/token) |

详见 `02-code-comments.mdc` 注释红线。

---

## 12. 一句话总结

> **每个函数不超过 30 行,每个函数只做一件事,每个变量自解释,每个错误都处理,每个魔法值都变成常量。**
