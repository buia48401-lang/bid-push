---
description: 规则体系元规则——本套规则的编写方法、触发模式、5 原则 / 3 反模式,供团队维护时遵循
alwaysApply: true
enabled: true
provider: project
---

# 规则体系元规则（Meta-Rules）

> 本文件是「写规则」的方法论,不是「代码规范」。所有其他规则文件编写、修改、审查时必须先读此文件。

---

## 一、规则文件结构

每条规则文件统一格式:

```markdown
---
description: <一句话说明>          ← 必须,供 AI 触发判断
alwaysApply: true | false         ← 是否每次自动加载
enabled: true
globs: **/*.vue                   ← 可选,按文件类型匹配(仅当非 alwaysApply)
provider: project
---

# <规则标题>

## <N>. <分章节>

✅ 正例:...
❌ 反例:...
```

**Frontmatter 字段说明**:

| 字段 | 必填 | 含义 |
|------|------|------|
| `description` | ✅ | **触发器**——AI 用此判断是否加载规则 |
| `alwaysApply` | ✅ | `true` = 每次会话自动加载,`false` = 需触发 |
| `enabled` | ✅ | 是否启用 |
| `globs` | ❌ | 仅在 `alwaysApply: false` 时生效,按文件路径 glob 匹配自动加载 |
| `provider` | ❌ | 通常填 `project` |

---

## 二、触发模式分级

| 触发模式 | 配置 | 适用场景 | 本套规则中的应用 |
|---------|------|---------|-----------------|
| **Always** | `alwaysApply: true` | 核心编码风格、通用安全红线、个人/团队偏好 | `00-05`(通用层) |
| **Agent Requested** | `alwaysApply: false`,无 globs | 方法论、工具流,AI 按需加载 | `10-12`(工具层) |
| **Auto Attached** | `alwaysApply: false` + `globs` | 框架/语言特定规则,按文件类型自动加载 | `lang-*` `fe-*`(框架层) |
| **Manual** | — | 临时任务级规则 | 不在项目级配置 |

**优先级冲突时**:Always > Auto Attached > Agent Requested > Manual;任务级规则 > 任何项目级规则。

---

## 三、规则编写 5 原则

| 原则 | 反例 → 正例 |
|------|------------|
| **① 具体** | "代码要简洁" → "每个函数不超过 30 行" |
| **② 可验证** | "写好代码" → "启用 TypeScript strict 模式" |
| **③ 有优先级** | 冲突时知道听谁:"性能和可读性冲突时,优先可读性" |
| **④ 有例外** | 说明何时可破例:"除非第三方库回调,否则都用箭头函数" |
| **⑤ 有示例** | 给正例 + 反例,让 AI 理解边界 |

---

## 四、3 大反模式(🔴 禁止)

### ① 规则太多
> 100+ 条细碎规则(2 空格缩进、单引号字符串)AI 记不住。
> **聚焦 20 条核心**,其余交给 **ESLint / Prettier / Black / Pylint**。
> 规则中可写:**"详见 `.eslintrc.json`"**。

### ② 自相矛盾
> 同时写"优先函数式"和"所有组件用 Class Component"——冲突。
> **写完通读一遍,检查逻辑冲突,确保规则自洽**。

### ③ 无上下文
> 只写"使用 useSWR"——AI 不知道啥场景用。
> **加场景限定**:"客户端数据获取用 useSWR;服务端用 Next.js 内置 fetch"。

---

## 五、规则验证方法

**核心方法学**:用同一需求让 AI 跑两次,对比两次输出是否风格一致。

```
让 AI 生成【描述】组件 / 函数
         ↓
跑第一次 → 输出 A
         ↓
跑第二次 → 输出 B
         ↓
A 与 B 风格趋同 = 规则稳定约束 AI ✅
A 与 B 风格差异大 = 规则失效,需加强 ⚠️
```

**规则不生效排查**:
1. **路径对不对**——必须是 `.codebuddy/rules/`,那个点不能少
2. **规则措辞够不够硬**——"代码要整洁"AI 会忽略;"每个函数必须不超过 30 行"才有约束力
3. **多用「必须」「禁止」「始终」** ——硬约束词,把规则当硬规则

---

## 六、代码风格模仿三步法

规则定底线,**Prompt 风格模仿术**处理细节。

| 步骤 | 动作 |
|------|------|
| ① 提供样本 | `@<文件路径>` 让 AI 学习典型文件:组件结构、Hook 封装、类型习惯、错误处理、注释风格 |
| ② 要求一致 | "按照刚才学到的风格生成,要求和原有代码**完全一致的代码风格**,从结构到命名,从注释到错误处理" |
| ③ 自检差异 | "对比原始文件和新生成的代码,**列出风格差异点并修正不一致**"——AI 主动发现比逐行审查高效 |

---

## 七、Prompt 模板库(放在 `.codebuddy/prompts/`)

团队共享以下模板(具体文件由开发者维护):

1. **创建组件** `<name>` —— 功能 / Props / 状态 / 交互 / 样式参考
2. **创建 API 路由** `<path>` —— 方法 / 入参 / 出参 / 校验 / 错误处理
3. **创建自定义 Hook** `<name>` —— 用途 / 输入 / 输出 / 副作用 / 清理
4. **Bug 修复** —— 现象 / 复现 / 期望 / 实际 / 根因假设 / 验证
5. **代码重构** —— 现状 / 目标 / 边界 / 验证方式

---

## 八、规则维护流程

```
团队成员建议新规则
        ↓
本人评估:是否落入 3 反模式?是否冲突?
        ↓
起草:按 5 原则(具体/可验证/有优先级/有例外/有示例)
        ↓
提交 PR + 描述触发场景
        ↓
架构师审查 → 合入 main
        ↓
进入规则体系,生效
```

**红线**:
- 🔴 不要把「团队 / 个人临时约定」上升为「项目级规则」
- 🔴 同一类规则不要在多个文件中重复声明(只留一份,在元规则里索引)
- 🟢 规则文件中遇到项目特定细节时,改写到子文件,不要污染通用规则

---

## 九、本仓库实际生效的规则索引

### 通用层(本目录,已按本项目裁剪)

| 编号 | 文件 | 触发 | 主题 |
|------|------|------|------|
| 00 | `00-rule-meta.mdc` | Always | 元规则(本文件) |
| 01 | `01-coding-style.mdc` | Always | 编码总则 |
| 02 | `02-code-comments.mdc` | Always | 注释规范(TS / SQL) |
| 03 | `03-ai-safety.mdc` | Always | AI 操作安全红线 |
| 04 | `04-api-design.mdc` | Always | API 设计总则(含本项目差异) |
| 05 | `05-modify-safety.mdc` | Always | 代码修改安全红线 |
| 10 | `10-troubleshooting.mdc` | Agent Requested | 问题诊断决策树 |
| 11 | `11-batch-ops.mdc` | Agent Requested | 批量操作三步法 |
| 12 | `12-self-check.mdc` | Agent Requested | 变更自检(已适配本项目) |
| fe-react | `fe-react.mdc` | Auto `**/*.ts(x)?x,**/*.js(x)?x` | React / Next.js 规范 |
| fe-css | `fe-css-tailwind.mdc` | Auto `**/*.css` | Tailwind / 设计 token |
| lang-sql | `lang-sql.mdc` | Auto `**/*.sql` | SQL 通用规范(含本项目差异) |

### 项目层(同目录 `proj-*.mdc`,冲突时**优先于**通用层)

| 文件 | 主题 |
|------|------|
| `proj-nextjs-stack.mdc` | 技术栈锁定、目录命名、接口信封与分页 |
| `proj-layer-boundary.mdc` | 分层单向依赖、Server/Client 边界、Supabase 双客户端选型 |
| `proj-domain-terms.mdc` | 业务术语 ↔ 数据库字段对照 |
| `proj-forbidden.mdc` | 禁止事项红线清单 |

**已裁剪**(与本仓库技术栈不符):`lang-java` / `lang-python` / `fe-vue` / `fe-uniapp`。
**优先级**:`proj-*` > 本通用层;同一约束两处都有时,以 `proj-*` 为准。
