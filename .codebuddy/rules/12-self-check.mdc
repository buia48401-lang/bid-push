---
description: 变更自检三模式（新增/迁移/合并）——完成代码变更后对照清单逐项核查,通用输出格式与风险评级
alwaysApply: false
enabled: true
provider: project
---

# 变更自检规则（12-self-check）

> **触发词**:「检查」「自检」「审查」「验证」「核查」「完工了吗」
>
> 完成代码变更后,**必须**按本规则自检一遍并输出结果。

---

## 一、三模式自检总览

| 模式 | 触发条件 |
|------|----------|
| **A. 新增 / 修改功能** | 创建或修改业务功能 |
| **B. 迁移模块** | 把代码从 A 系统 / 包迁移到 B 系统 / 包 |
| **C. 合并功能** | 两个相似功能合并为一个 |

> 不确定属于哪个模式?→ AI 判断后告知用户,让用户确认。

---

## 模式 A:新增 / 修改功能自检

### 1. 完整性检查(本项目适配,依据 proj-* 规则)

```
[ ] 分层正确:route.ts 只做「取参 → Zod 校验 → 调 lib/data」,信封与错误交给 withRouteHandler
[ ] 依赖方向单向:components/** 不得 import lib/data、lib/supabase;lib/**、types/** 不得 import app/**、components/**
[ ] 数据客户端选型正确:公告/详情/推送日志/抓取日志走 getServerClient(),订阅走 getAdminClient(),且在函数体内惰性获取
[ ] 只消费契约列:无 select('*'),分页用 .range() + count,关联名称用嵌套 select(无 N+1),大字段已截断
[ ] 未新增接口路径 / 错误码 / 数据表;信封 { code, message, data, traceId } 未改动
[ ] snake_case ↔ camelCase 映射只在 lib/data/mappers.ts;structured 键保持 snake_case
[ ] 业务中文字面量与色值未硬编码(引用 lib/constants/domain.ts;色值只写 globals.css token 处)
[ ] 数据库脚本:只新增 n8n/sql/003_xxx.sql,幂等 + 反向 SQL,未改动 001 基线与 docs/
```

### 2. 编译与测试(在 `application/` 下执行)

```
[ ] npm run lint 通过
[ ] npm run typecheck 通过
[ ] npm run build 通过(含 Next 路由类型校验)
[ ] npm run test 通过(Vitest)
[ ] 关键业务流已本地自测
```

### 3. 遗留问题检查

```
[ ] 无未处理 TODO / FIXME / HACK
[ ] 无硬编码魔法值 / 业务中文字面量 / 色值(一律走常量与 token)
[ ] 异常处理完整,不只有正向逻辑(structured 解析失败须降级为 null,不抛错)
[ ] 关联模块已同步修改
[ ] 注释规范(02-code-comments)
[ ] 已实现的查询体删除了对应 unimplemented() 调用
```

### 4. 风险评级

| 风险 | 说明 | 处理要求 |
|------|------|----------|
| 🔴 高风险 | 核心表结构修改 / 认证鉴权逻辑 / 金额计算 / 支付银行接口 | **必须马上处理** |
| 🟡 中风险 | 新增模块 / 跨模块调用 / 字典配置变更 | 建议优先处理 |
| 🟢 低风险 | 纯展示 / 只读查询 / 不涉及写操作 | 可延后 |

---

## 模式 B:迁移模块自检

### 1. 包路径与结构

```
[ ] 旧包路径已清理(团队约定的废弃包名)
[ ] 新包路径符合本项目规范
[ ] 旧代码引用已替换为新包路径
[ ] 配置文件 / 框架注解 / 路由表同步更新
```

### 2. 数据迁移

```
[ ] 迁移脚本已放置约定目录(如 tools/migration/ 或 sql/migration/)
[ ] 旧表行数 vs 新表行数一致
[ ] 关键字段非空率接近
[ ] 字典值映射正确
[ ] 数据迁移脚本已执行并验证(有 verify 脚本或对比报告)
```

### 3. 接口与前端对齐

```
[ ] 新系统接口 URL 与前端对齐
[ ] 旧接口已下线或已做兼容(必要时保留)
[ ] 双边功能对比完成(见下方对比表)
```

### 4. 双边功能对比表(模板)

| 功能点 | 旧系统 | 新系统 | 是否完成 | 备注 |
|--------|--------|--------|---------|------|
| 列表查询 | ✅ | ✅ | 完成 | — |
| 新增 | ✅ | ❌ | 未完成 | 缺少 XX 字段 |
| 编辑 | ✅ | ✅ | 完成 | — |
| 删除 | ✅ | ✅ | 完成 | — |
| 详情 | ✅ | ✅ | 完成 | — |
| 导出 | ✅ | ❌ | 未完成 | — |
| 导入 | ✅ | ✅ | 完成 | — |

---

## 模式 C:合并功能自检

### 1. 被合并 / 删除功能记录(必填)

| 功能 | 原位置 | 现位置 | 状态 | 操作人 | 删除 / 合并原因 |
|------|--------|--------|------|--------|----------------|
| 合同列表 | `old-contract/` | `crm/contract/` | 已合并 | — | 合并到 CRM |

> **必须**输出文档并记录操作人,便于追溯。

### 2. 合并后检查

```
[ ] 冲突代码已解决(分支合并时的冲突)
[ ] 重复 Bean / 重复接口 / 重复配置已清理
[ ] 编译通过
[ ] 核心流程已回归测试
[ ] 旧入口已重定向或移除
[ ] 旧权限标识已下线或挂到新入口
```

### 3. 合并冲突分类清单(模板)

```markdown
## 合并冲突清单

| 类型 | 数量 | 示例 |
|------|------|------|
| 包路径冲突 | 5 | `com.old.order` vs `com.new.order` |
| 数据库表冲突 | 3 | 主键生成策略不一致 |
| 权限标识冲突 | 2 | 都有 order:create,合并保留其一 |
| 配置项冲突 | 1 | 配置键名差异 |
| 命名风格冲突 | 多 | 驼峰 vs 蛇形 |
```

---

## 二、通用输出格式(🔴 强制)

每次执行自检后,**必须**按以下格式输出:

```markdown
## 自检结果

- 模式: A 新增 / 修改功能
- 风险评级: 🟡 中风险
- 已通过项: 16 / 18
- 未通过项:
  1. [ ] select('*') 未改为显式契约列
  2. [ ] npm run build 未通过
- 建议: 优先改显式列并修复构建错误,再重新验证
- 验证方式: `npm run lint && npm run typecheck && npm run build`
```

---

## 三、自检未通过的处理

### 3.1 处理流程

```
未通过项
  ↓
[1] 区分阻塞项 vs 非阻塞项
    - 阻塞项:阻塞上线 / 合入 → 必须立即修
    - 非阻塞项:优化项 → 记录到 TODO,排期修
  ↓
[2] 阻塞项:立即修复 → 重新走自检
  ↓
[3] 非阻塞项:登记到 backlog,标记 owner
```

### 3.2 阻塞项示例(🔴 不可延后)

- `npm run lint` / `typecheck` / `build` 任一不通过
- 单元测试不通过
- 违反分层边界(组件直连数据层 / service_role 泄漏到客户端)
- 违反契约(新增接口 / 错误码 / 改信封)
- 物理删除 / 数据未备份

---

## 四、自检时机

| 时机 | 是否必须自检 |
|------|--------------|
| 提交 PR 前 | ✅ 必须 |
| 合并到 main / develop | ✅ 必须 |
| 上线前(发布 check-list) | ✅ 必须 |
| 完成 AI 提的多个 todo | ✅ 必须 |
| Bug 修复 | ✅ 必须(走模式 A 但偏修复) |
| 重构较大功能 | ✅ 必须(走模式 A + 风险升级) |

---

## 五、自检结果的置信度

| 维度 | 检查方法 |
|------|----------|
| **完整性** | 团队 2 人对照清单确认 |
| **编译 / 测试** | 自动化 CI 流水线 |
| **可观测** | 关键接口 / 错误率监控 |
| **回归** | 跑相关 E2E / 接口测试套件 |

> AI 输出的「自检通过」**不**等于上线通过——AI 不可能替代 QA 全链路验证。

---

## 六、一句话总结

> **改完代码先自检,改需求先评估,删代码先查引用,发版前必走灰度**。
